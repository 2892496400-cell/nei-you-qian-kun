# 内有乾坤

这是一个可直接部署到 GitHub Pages 的静态登录页。

## 全网修改密码

初始密码是 `000000`。之后在 GitHub 仓库中打开 `password-config.json`，点击编辑图标，将 `password` 的值替换为新密码并点击 **Commit changes**。GitHub Pages 重新部署后，所有访客刷新页面就会读取新的密码。

> 这是静态网页，密码配置可被有技术能力的人从浏览器开发者工具中读取；它适合做提示式、小范围的页面门槛，而不适合保护真正私密的信息。
